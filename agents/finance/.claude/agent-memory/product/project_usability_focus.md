---
name: project-usability-focus
description: Current product focus: usability — mini app UX and /balance improvement. Pockets name stays.
metadata:
  type: project
---

Current priority is **usability**, not new features.

**Pockets:** keep the name — it's distinctive, already in DB/API/tab, better than "budgets" or "envelopes". No rename.

**`/balance` command:** current output is flat text (`💳 Name: 12,345.00 UAH`). Planned improvements: total net worth line at top, group by currency, pockets summary below accounts.

**Mini app:** the main usability target. See [[project-miniapp-status]] for options.

**CLI surface** (3 registered commands): `/finance_app`, `/balance`, `/sync`. Pockets domain is fully built in backend but NOT exposed as a bot command yet.

**How to apply:** When ideating or planning, prioritize polish and usability over new domain features. Phase 3 new domains (Debt, Goals, Trips, etc.) are lower priority than making the existing surface feel good.
